# Skills

This repository keeps generic Skill definitions here.

Current Skill:

- `ai-task-manager`

The Skill itself is tool-neutral at the repository level.

Users should install the Skill into whatever AI tool they use.

If they change AI tools later, they only need to reinstall the Skill in the new tool. The `ai-handoff` CLI remains the same.
